# User Settings

## Storage

User settings are stored in a local file that is excluded from version control (`.gitignore`). This keeps personal preferences private and avoids polluting the shared repository with per-user configuration.

## Discoverability

A CLI command displays the current user settings. If a user has not yet configured their preferences, the agent surfaces the relevant options at the appropriate moment rather than requiring upfront configuration.
