{
  "name": "desktop",
  "private": true,
  "version": "0.26.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:with-bridge": "npm --prefix .. run build && npm run dev",
    "build": "tsc && vite build",
    "build:with-bridge": "npm --prefix .. run build && npm --prefix .. run desktop:prepare -- --bundle-node && npm run build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "@tauri-apps/plugin-updater": "^2.10.1",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/node": "^26.1.2",
    "@types/three": "^0.185.1",
    "typescript": "~7.0.2",
    "vite": "^8.1.5"
  }
}
